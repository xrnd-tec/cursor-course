# practice/

ここに小さな JS があります。ドキュメントの実習から触ってください。

| ファイル | 向いている練習 |
|----------|----------------|
| `calculator.js` | Agent で関数実装、Ask で説明 |
| `greeter.js` | Tab / `Cmd+K` |
| `cart.js` | `@` 参照、バグっぽい TODO |
| `index.js` | 総合実習のエントリ |

ESM です（リポジトリ直下の `package.json` に `"type": "module"` があります）。
`node practice/index.js` でそのまま実行できます。
