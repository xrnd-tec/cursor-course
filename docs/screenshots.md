# スクリーンショット撮影台本

授業スライド用の画面キャプチャを、進行台本（[`courses/4sessions/`](../courses/4sessions/)）の順に撮るためのリスト。

撮る人が上から順に実行すれば、スライドに必要な画像が過不足なく揃うことを目指す。
第1回で **16枚**、第2回で **6枚**。撮影の所要時間は第1回で40〜50分（Agent の応答待ちを含む）。

---

## 撮影前の準備（全体で1回・10分）

画像の見た目が揃っていないとスライドが安っぽくなる。**最初に固定して、途中で変えない。**

### 環境を固定する

| 項目 | 設定 | 理由 |
|------|------|------|
| ウィンドウサイズ | **1280 × 800** に固定 | 全画像の縦横比が揃う。スライドに並べたとき破綻しない |
| テーマ | ダーク / ライトのどちらかに統一 | スライドの背景色に合わせて先に決める |
| エディタのズーム | `Cmd + 0` で既定に戻す | 拡大したまま撮ると文字サイズがバラつく |
| 通知 | おやすみモード ON | Slack 等のバナーが写り込む事故を防ぐ |
| サイドバー | ファイルツリーだけ表示 | 拡張機能パネル等が写るとノイズ |

### 写り込みを消す

- **他のリポジトリ / 業務ファイル**をエディタのタブから全て閉じる
- Cursor 右上の**アカウントアイコン・メールアドレス**が写る構図を避ける
- ターミナルを写す場合、プロンプトにマシン名やユーザー名が出ていないか確認する

### リポジトリを初期状態にする

実習の初期状態（`applyDiscount` が未実装）でないと、台本どおりの画面にならない。

```bash
git switch main
git pull
git status          # クリーンであること
```

`practice/calculator.js` の `applyDiscount` が TODO のままであることを目視で確認する。

### 保存先と命名

- 保存先: `docs/images/session-01/` · `docs/images/session-02/`
- 命名: `s01-03-mode-menu.png`（`s{回}-{通し番号}-{内容}`）
- 撮影は `Cmd + Shift + 4` → `Space` → ウィンドウをクリック（ウィンドウ単体で影付きに撮れる）
- 1枚 500KB を超えたら縮小する（リポジトリが膨らむため）

---

## 第1回：基本操作（16枚）

対応する台本: [`session-01.md`](../courses/4sessions/session-01.md)

### 0:00 セットアップ確認

#### s01-01 — 開いた直後の全体画面

- **狙い**: 「今日使う画面はこれです」の1枚。スライドの導入で各部に矢印を引く
- **作り方**: `cursor-course/` を開き、`practice/calculator.js` をサイドバーから開く。Agent パネルは**閉じた状態**
- **写す範囲**: ウィンドウ全体
- **注意**: この1枚だけは矢印注釈を入れる前提なので、余白が潰れていないこと

### 0:20 前半① — Cursor の3つの手段

#### s01-02 — Tab の提案が出ている瞬間

- **狙い**: 「Tab は"続き"」の説明
- **作り方**: `applyDiscount` の中で `const discounted =` あたりまで打ち、灰色の提案が出た状態で止める
- **写す範囲**: エディタ部分を中心に（全体でなくてよい）
- **注意**: 灰色の提案文字が読める解像度で。小さすぎたらエディタだけ切り出す

#### s01-03 — Cmd+K のインライン入力欄

- **狙い**: 「Cmd+K は"ここだけ"」の説明
- **作り方**: `applyDiscount` 関数全体を選択 → `Cmd + K` → 入力欄が出た状態（まだ送らない）
- **写す範囲**: 選択範囲と入力欄が両方入るように

#### s01-04 — Cmd+K の実行結果（diff）

- **狙い**: インライン編集が「その場で差分になる」ことを見せる
- **作り方**: s01-03 の入力欄に `コメントを日本語に直して` と送り、差分が出た状態
- **注意**: Accept する前に撮る

#### s01-05 — Agent パネルを開いた直後

- **狙い**: 「Agent は"会話しながら"」の説明。パネルの各部を指す
- **作り方**: `Cmd + I` でパネルを開く。まだ何も送らない
- **注意**: モード表示（入力欄の左）が見えていること。ここが後の説明で効く

### 0:30 前半② — Ask → Agent 切替と `@`

#### s01-06 — モードメニューが開いた状態 ★重要

- **狙い**: 「モードは4つある」の説明。コース中で最も使う1枚
- **作り方**: 入力欄で `Shift + Tab` を押し、メニューが開いた状態
- **注意**: Agent / Ask / Plan / Debug が全部読めること。**メニューは操作すると消えるので撮り逃しに注意**

#### s01-07 — Ask モードになった状態

- **狙い**: 「モードが変わったことは、ここを見れば分かる」
- **作り方**: Ask を選んだ後、入力欄の左のモード表示が `Ask` になっている状態
- **写す範囲**: 入力欄まわりを**拡大して**切り出す（全体だと小さくて読めない）

#### s01-08 — `@` のサジェストが出ている状態 ★重要

- **狙い**: 「`@` でファイルを指定する」の説明
- **作り方**: 入力欄で `@practice/calc` まで打ち、候補リストが出た状態
- **注意**: 候補にファイル名が読めること

#### s01-09 — Ask の回答が返ってきた状態

- **狙い**: 「Ask は答えるだけで、ファイルは変えない」の証拠
- **作り方**: `@practice/calculator.js このファイルの役割を3行で説明して` を送り、回答が出きった状態
- **注意**: **差分が出ていないこと**がこの画像の主張。エディタ側も一緒に写す

#### s01-10 — Agent モードに戻した状態

- **狙い**: s01-07 と並べて「切り替わった」を見せる
- **作り方**: `Shift + Tab` で Agent を選び、モード表示が `Agent` になった状態
- **写す範囲**: s01-07 と**同じ切り出し範囲**にする（並べたとき比較になる）

#### s01-11 — Agent が実行中の状態

- **狙い**: 「待っている間はこう見える」。受講者が不安になるポイント
- **作り方**: `applyDiscount` の実装依頼を送った直後、処理中の表示が出ている状態

### 0:45 前半③ — 差分の確認・Accept / Reject

#### s01-12 — diff ビュー ★最重要

- **狙い**: 「Accept の前に必ず diff を読む」— コース全体で一番大事なスライド
- **作り方**: Agent が `applyDiscount` を書き終え、緑（追加）と赤（削除）が出ている状態
- **注意**: 緑と赤が**両方**写っていること。追加だけの差分だと説明が弱い

#### s01-13 — Accept / Reject ボタン

- **狙い**: 「ここで受け入れるか戻すかを決める」
- **作り方**: s01-12 の状態で、ボタンがある部分を**拡大して**切り出す
- **注意**: ボタンの文字が読めること

#### s01-14 — Restore Checkpoint

- **狙い**: 「行き過ぎても戻せる」の安心材料
- **作り方**: 送信済みメッセージにホバーし、`Restore Checkpoint` が見えた状態
- **注意**: 台本の旧版にあった「カメラアイコン」は誤り。**この表示が正しい**

#### s01-15 — Accept 後のファイル

- **狙い**: 「実際にコードが変わった」
- **作り方**: Accept 後の `calculator.js`。`applyDiscount` が実装済みになっている

### 0:55 後半 — 課題A

#### s01-16 — 課題Aの diff

- **狙い**: 受講者が自分でやる課題のゴール画像。「こうなれば正解」
- **作り方**: `cart.js` の `getSubtotal` に、price / qty 欠落をスキップする処理が入った diff
- **注意**: **空カートの分岐を足させないこと**（既に 0 を返すため。台本修正済み）

---

## 第2回：バイブ → 仕様駆動（6枚）

対応する台本: [`session-02.md`](../courses/4sessions/session-02.md)

この回のスライドは**比較**が命。バイブ版と仕様版を必ず同じ構図で撮る。

#### s02-01 — バイブで作った神経衰弱（動作中）

- **狙い**: 「曖昧な依頼でも、動くものは数分で出る」
- **作り方**: `神経衰弱ゲームを作って。HTML + JS で、ブラウザで遊べるようにして。いい感じに。` の結果をブラウザで開いた状態
- **写す範囲**: ブラウザウィンドウ

#### s02-02 — 依頼3の diff（変更行数が見える状態）★重要

- **狙い**: 「つぎはぎで積み上がる変更量」。第2回の主張はここに乗っている
- **作り方**: 「制限時間＋デザインもかっこよく」を送った後の diff。**変更行数（+/-）の表示が写るように**
- **注意**: 行数が読めないと意味がない。数字部分を拡大した2枚目も撮っておくと安全

#### s02-03 — 計測表を埋めた状態

- **狙い**: 授業中に全員で埋めた表をスライドに残す
- **作り方**: 板書 or スライドに書き込んだ計測表（依頼1〜3 × 変更行数・3機能の生存）を撮る
- **注意**: 画面キャプチャでなく写真でもよい

#### s02-04 — 仕様を渡した直後の Agent 画面

- **狙い**: 「新しいチャットに仕様を貼って渡す」型を見せる
- **作り方**: 新規チャットに最小仕様を貼り付けた状態（送信前でも可）

#### s02-05 — 仕様版の追加依頼の diff ★重要

- **狙い**: s02-02 と**並べて**「同じ機能追加でも差分がこれだけ違う」を見せる
- **作り方**: 仕様版に手数表示を追加した diff。変更行数が写るように
- **注意**: s02-02 と**同じ切り出し範囲・同じ拡大率**で撮る。並べたときに比較にならない

#### s02-06 — 「3回目以降は 0.5 秒」の挙動比較

- **狙い**: 仕様にしか書いていないルールが、バイブ版には存在しないことを見せる
- **作り方**: バイブ版と仕様版の両方で3回ミスしたときの挙動。動画が難しければ、該当コードの有無を diff で見せてもよい
- **注意**: この1枚が「仕様を書く意味」の最も強い証拠になる

---

## 撮り終わったら

1. `docs/images/` に置いて命名規則どおりにリネーム
2. **このチャットに画像を渡す** — 台本の記述と実機が食い違っていないか照合する
3. ズレがあれば台本側を修正してからスライドに組む

撮影中に「台本どおりの画面にならない」箇所があれば、それは**教材のバグ**なので必ず記録しておくこと。スライドを作り直すより台本を直す方が先。

## 関連

- 第1回の台本: [`../courses/4sessions/session-01.md`](../courses/4sessions/session-01.md)
- 第2回の台本: [`../courses/4sessions/session-02.md`](../courses/4sessions/session-02.md)
- 制作タスク: [`tasks.md`](tasks.md)
